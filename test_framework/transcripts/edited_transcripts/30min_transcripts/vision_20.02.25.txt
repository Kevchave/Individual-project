Good morning. Uh. Let's start.
So in last lecture, uh, was, uh, we start a topic on self-supervised learning, and we mentioned that we can even without labels.
We can leverage the similarity and the contrast in the data to define some loss function and start training the neural network.
And in this way that this part, this, uh, convolution neural networks or other models here, they can be trained.
Um, this is uh, for this example that, uh, we assume we have two samples for the sample dog that we can create two versions dog.
And the features for these two version of the dog should be similar to each other.
This is one part of the loss function. Uh, and then we also have, uh, these comparison between the dog and the sofa.
And that feature should be dissimilar. And this second part of the loss function we want to define.
So that's the high level idea. And now we can look at uh the exact loss function we're going to use for training this model.
So here we to for to train this uh, a model that, uh, we need to have, uh,
loss function that can maximise the agreement between z I and z j and z a and z z z are two versions of this talk, and they are uh, to.
You can apply data computation to the same image of dog creates two different versions, and there's two different versions.
When it goes through these convolutional neural networks, the the network can generate the features which are z,
I and j here at meantime one to minimise the agreement between z I and other z.
And this are that is a feature of that. So for image.
And the rough idea is that maybe we can put this to loss function into one equation, which is this equation that h is a fraction at the numerator.
This is something we want to maximise. Uh, so we want to maximise the the similarity between z and that j.
And at the denominator it is uh all the other, all the similar sums for all different paths.
And we want to throw in these uh, uh denominator that uh, it contains the simultaneous z,
I and J and also the similarity between z I and all the odds is that,
um, if we can minimise the similarity between z I and, um, that this fraction will be it become uh, will be also be maximise.
And so that's a rough idea. And the here that we are using this idea, however, that we will try to put this term into the,
uh, into the cross-entropy loss, which is, uh, classification loss.
And we know that if we want to perform classification tasks that once you define the, uh, different classes,
we will know, uh, if there are two classes, one class means, uh, the positive class, uh, uh, that are.
And that session will be similar to each other. We can call this, uh, positive path.
And the second class is Z, R and others, we can call them negative paths.
If you have these two classes, we can use, uh, cross-entropy loss to define the loss function.
And to define this loss function that, uh, we need to draw some samples from the images.
And if you if you have a dataset with a billion images,
you cannot like utilise all these one colour images in one pass, but instead that we each during each iteration,
we randomly throw a batch of a big and images and we can apply the augmentation to these big, um, images so that each image will have two versions.
We have a two times, and for each sample I that we would regard this sample I and its own augmentation j as one positive pair.
And this sample. Oh with all the other two times m minus one samples they will form the negative pairs.
And in this way that we can define, uh, a binary classification problem to tell the difference between positive pairs uh,
against negative pairs, um, to perform binary classification, we can use the cross-entropy loss function.
And at the bottom you can see for example, suppose pick n equals four from the original four images.
After augmentation you will have eight images um each of number a form the pair with uh its corresponding even number.
So actual one and two. They are coming from the same original image three and four.
They are also coming from the same original image. And for this interpretation we can maybe define three and four as one called the path.
We colour coded them using blue. And then if we pass away with two, three um two are from different images.
So they should form a net path three and one also from a negative pair.
And three with five is also a negative. And in this way we can have a number of negative paths and one positive path.
And after that we can go down the cost of loss which would look like this.
So we know that, uh, cross entropy loss is, uh, a function that can, uh, measure the distance between two probabilities.
So if you have the. Two probabilities p and q.
That p is defined as. One.
Negative sum of pi. Cons logarithm of xi.
Here PR means the ground truth probability.
QR means the predicted probability by the neural networks, and if it is for the ground truth,
if it is, uh, binary classification problem, it means for some samples, uh, the.
Oh point. Four.
We can use the. One hot encoding to represent pi, which means it might be for class the for the files class.
The ground truth is 100% and for a second class the ground truth is 0%.
That is the one hot encoding for PR. Um, if we use one hot encoding, it is also not the end.
This term it become just some pii which is times logarithm of xi.
And this means that the pii which uh falls into class one, which well we have pii equals one.
And so the cross entropy loss function would look like this minus logarithm of something.
And that is exactly what we are looking here. Um, and also we notice that, uh, within the logarithm, it's uh quite complicated.
Um, uh, but uh, the idea is very simple.
The most in part is just a similarity between two, uh, two pairs of images between their features.
And then we have this exponential term, which is the softmax function that can transform the original similarity into some probability.
So if we have the original. Smart.
Looks like this. Some similarity between z I and z z.
Uh, if we take z, uh, simpler notation, let's just quit.
Uh oh. And we know that, uh, one if, uh, is if we want to transform a and also the other A for,
for the similar patterns that are and other z into a probability vector, you can use the softmax function.
And what the softmax function does is just to make sure that this number is a positive number.
So we'll use the exponential to the power of A.
And instead of saying the softmax does is to make sure at the end.
If you have, uh. If you have none of these arrays, you want to make sure that the sum is equal to one.
So in order to achieve that, you need to have this the denominator that which is the sum of all the exponentials.
And that is what we are looking at here.
So here that at the bottom this is a cross entropy loss function for binary classification problem between the quality of pairs and negative paths.
And for each pair of uh image features that we first calculate the similarity.
And then we transform this similarity into a softmax of probability.
And there is another term here which is this towel. And this tie is called the temperature parameter.
It controls how how sharp how steep your exponential function will look like.
And at the bottom it's a sum of all the exponentials.
And that is the indicator function here which said this letter looks like one.
And it's just, uh, means that we have, uh, when the code goes through, we'll start from zero, uh, one all the way to, to n.
And the only thing we're not going to to choose it is R itself.
So one, we clear out all these paths or we're only if we're looking at sample three.
If we're looking at the samples, we swear we are not from a par with it itself.
It will only from par with other samples. And that has a what this, uh, indicator function is doing.
And this function.
It looks like a cross entropy function, but it also have this, uh, temperature here, which controls, uh, the softness of the exponential term.
Uh, so, uh, we gave its name, which is called normalised temperature, scaled cross entropy loss question.
Because the scale of the temperature. Okay.
So the temperature is.
So if you have a. If you have a very large temperature like, uh, 1 million or 1 billion, if time equals 1 billion,
then all these similarity come right is divide by the temperature will be become very, uh, small number close to zero.
I mean, that way that the probability for all the paths will be looking similar to each other.
However, if Tao is a very small number, such as nought point, nought nought one,
then when the similarities divide by this very small number, it will be exaggerated.
And that means that if. For example, if this is samples why is sample for.
They have some similarity and between samples way and sample to the.
Also we can also calculate the similarity. And if this tile is very small.
It will exaggerate the difference between these two similarities.
And while we choose these samples that, uh, in this example, we define the part three and four as a positive sample.
And all the other pair. 3231356 as negative pairs.
And because that is the eight samples here, we can have a number of ways to define the positive versus negative.
And for this case that we we define one positive versus six negatives the positive
between 3 and 4 we can calculate the similarity uh divide by Tao and then this number.
Well then we will apply the soft maximum function to this number.
And this number is called the logits largest means uh, when you are using the softmax function.
Which is uh, this function, this oh, this power of the softmax is called the logits.
Um, after you apply softmax on function to the logits, you get the probability.
Um, also, we can define the ground truth label for the positive power.
Positive power label one and negative of paths will have label zero.
And after you calculate uh this column the largest column, and define the labels column,
you can call the cross entropy function from PyTorch to calculate this contrastive loss.
And this as a for uh this positive path.
We can also define other policy paths. For example, we can also define a positive power to be between 4 and 3.
Um, to defines a negative path to be uh 424145.
And in this way you can calculate this cross entropy function gain.
And at the end you can add all these cross cross entropy loss functions.
Questions? Yeah. Uh, could you and we should get out of there so that the general patterns when you said,
uh, if you only consider a specific person like, uh, how do you manage this situation?
Uh, so we've never seen that. Consider, uh, the last number of courses.
You can see there are two images that are similar but not the same.
Uh, so, uh, so how would you like to view the patterns of this information that are similar but not the same?
Okay. Yes. So we can go from, uh.
Let's suppose that, uh, we're looking this, uh, this kind of images.
So this is much. Here.
We have a bar in the image. This is another image.
And they look similar to each other. But we also have the image which looks different between it because the orientation of the bar is different.
And initially that we don't have any model here.
And we need to define some model which can be a thing, which can be something else to learn, uh, pattern from this, these paths.
And here we assume that this has a positive colour. And this is a negative pair.
Um, maybe they contains ten by ten pixels.
So this time that by pixels will be our, uh, input data x.
And this function, suppose it has only one layer of convolution.
It will look like this. So we'll calculate some weight multiplied with this x.
And then we apply activation function. Then we get some output.
Initially we have x but we don't know w.
So we need to adjust this w so that when w is multiplied with x this number can
tell the difference between these kind of patterns against those kind of patterns.
And in this process that W will learn how to detect these kind of bars or edges and orientations,
and so that it can utilise these, uh, edge information to tell the difference.
But that is a for a very, uh, simple image.
And you right here, when we look at the image of a dog versus the image of so far, we need to define, uh,
a convolutional neural network with a few layers so it can learn features from both images to understand the difference.
And as long as you tell the network that between the dog and the sofa, this should belong to different classes.
But using just this very simple, uh, supervisory information, at the end, the network can learn how to define W.
Okay. Um, for this, uh, eight samples here, in this example, we can find lots of different ways to define the positive versus negative path.
So actually that each time you can define one number which is both both function number.
And then you find its corresponding uh or even number to form a positive pair.
And between these both constant number and all the orange number you know from the next pass.
And if you do this for all possible combinations at the end,
you can write down the total loss function, which is the sum of several cross entropy loss,
and that is the loss function we use to, uh, train, uh, this convolutional neural network so it can understand the difference between images.
And the in this entire training process, we don't need to use any human annotations.
And after this, uh, image encoder F is trained.
How do we know that it's working well? So there are two ways to assess its performance.
One way it's called the linear probing, which means that, uh, we only add a linear layer onto the network and then evaluate its performance.
And in linear probing we phrase encode the parameters.
If we have some input image. And during contrastive learning what we are trained is this f function.
And this f function can learn some features. And this f function it looks like this.
If you have zoom in it, it will look like it contains the first convolution layer, then second convolution layer, then some maybe max pooling layer.
At the end it gets a feature. So here that at the bottom we have the input image.
And for this all these blue blocks they are trying to during contrastive learning
and for linear probing what we do is that based on the final output of f function.
We add a fully connected layer similar to multi-layer perceptron.
And the number of neurones in these fully connected layers is the sum of the number of classes you want to classify from a of classification task arm.
So if you are testing this for ImageNet classification, then you need to put 1000 neurones here for the 1000 classes.
And then you the phrase this part.
So all the parameters in these the blue blocks, they are not changed.
They are they are frozen. The only parameter we try is the connection weights between this blue layer and the last layer.
And this is called the linear probing and art.
You adjust the weights between this blue block and this green block.
You can evaluate the classification performance. And, um, on the right you can see the, uh, performance of linear probing for this method.
So, um, the image net takes that after the inner probing, you can see like around 75% accuracy and all the other dots,
uh, the, uh, the method it compares to, which are also, uh, self-supervised learning methods.
And if you use the fast learning methods, the accuracy you can see of is is shown here.
It's about like 77%. It's actually that the performer is quite close to each other, however,
that here we don't need to use any, uh, human annotations in training the model.
The second way to the balance performance is called fine tuning.
For fine tuning that. We are training others in here.
So we are turning the, the the weights for this, the last layer and also the weights for all these blue layers.
So it's the reason it's called fine tuning is that for all these blue layers the parameters are rather pre-trained.
So assume let's see. We have some very good initial values.
So we don't need to train it for a very long time.
Maybe you just train for one epoch or ten epoch that it is sufficient for this green block.
Uh, we don't have good initial values, but we still have to train you on this process.
And when you have fine tuning all these layers, you still need to use a small, uh, that's that with human provided labels,
like, because this, this time that the majority of networks have already been trained.
So you don't need to use a very large, uh, training set with labels, maybe like, uh,
1000 images with human labels will be already sufficient to train or to fine tune this neural network.
And after fine tuning again, that you can compare the performance of this contrast method,
which is at the bottom of this table, to the other methods and also to the supervised method.
So just learning, uh, all this, uh, so, um, I said it was proposed in 2020.
And after that, also, there are some other contrastive learning approaches being explored, including Biol, Duino or Duino version two,
etc. we are not going into details, but most of these methods will follow this same framework, which is at the bottom right.
How our biology would maybe change the component for this UQ uh f function, or define a different way to uh, for this loss function.
Um, and uh, to improve and then evaluate whether by doing so that we can improve the performance for self-supervised learning.
So after contrastive learning, uh, next approach we are going to describe is called mask image modelling.
This is also a way to train a model without human annotations.
So it is also a type of self-supervised learning technique.
And this technique, uh, was inspired by research in natural language processing and in natural language processing.
A very common way to train a method is to mask, in some words out, then ask model to predicting these, uh, missing words.
So it's a bit like, uh, the English exam papers you were taken during high school that for some question that, uh, that was a blank.
And you need to fill in the word, uh, in that blank so that it can complete the sentence.
And in natural language processing that either for bad or for GPT, that a lot of self-supervised learning was done uh,
to to pre-trained model to get good weights without human annotations.
And we are going to first briefly review the ideas in these natural language processing techniques.
And we assess how we can adapt these techniques to computer vision problems.
So the first message that we are going to look at is called GPT, which stands for a generative pre-trained transformer.
So from its name we can see it's you know, it has uses, uh, a transformer architecture, which means you have a number of layers.
Each layer has this self-attention mechanism.
And in terms of training this kind of GPT model, there are two stages.
The first stage is self-supervised learning.
So you need to pre-train this model. And during pre-training we don't need to use any human labels.
We just ask the model to predict the next word in the document or the next tokens.
Um, by doing this very simple task of the next, how can prediction you can get good weights for all these, uh, attention layers.
And up this pre-training stage there is a fine tuning stage.
So in fine tuning stage, we need to use some knowledge from human.
So we can either use, uh, human labels.
So if you have some sentences which are questions and if you human provide ground truth answers to these questions,
you can use these pairs of questions versus answers to train, uh, the GPT model.
But there are also other ways to utilise human knowledge, such as you can use, uh, human feedback.
So in this process that you don't need the human to create the ground truth answers for any question,
you just provide the answer generated by the GPT model and the human with the rates, the quality of these.
Uh, answer. So if the answer is good, you can give it a mark of ten.
If the answer is not good, is not aligned with human knowledge, you can give it a rate of, uh, zero.
And in this way, this kind of a human feedback can also be defined.
Uh, we can also define a loss function to, to propagate these kind of human feedback, to fine tune the, uh, parameters in the GPT model.
And here you can, uh. You can in this diagram, uh, we show you that, uh, how we can, uh, define a model to predict the next token.
So we will assume that some model in, uh, in the middle, which are these blocks?